import { Meta, Story, Canvas, Source, ArgTypes } from '@storybook/blocks';
import * as SwitchStories  from './switch.stories.tsx';

<Meta title="Components/Inputs/Switch"  />

# Switch
A control that allows the user to toggle between checked and not checked.

<Canvas>
    <Story of={SwitchStories.Example} />
</Canvas>

### Usage
<Source code={`
import React from "react";
import { Switch, SwitchProps } from "@envio-ui/design";

export const Example: React.FC<SwitchProps> = () => {
  const [value, setValue] = React.useState<boolean>(true);

  return (
    <Switch
      label="Place label here"
      value={value}
      onChange={() => {
        setValue(!value);
      }}
    />
  );
};
`} of={SwitchStories.Example} dark language="tsx" />

### Props

<ArgTypes of={SwitchStories.Example} sort="alpha" />
